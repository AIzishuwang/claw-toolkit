[package]
name = "sse-parser"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Incremental Server-Sent Events parser that handles cross-chunk boundaries"

[dependencies]
